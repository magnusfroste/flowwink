# =============================================================================
# PEZCMS ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your values
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# FRONTEND ENVIRONMENT VARIABLES (Required)
# -----------------------------------------------------------------------------
# These are used by the Vite frontend. Get them from Supabase: Settings → API

# Your Supabase project URL
VITE_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"

# Your Supabase anon/public key (safe to expose in frontend)
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key-here"

# Your Supabase project ID (the YOUR_PROJECT_REF part of the URL)
VITE_SUPABASE_PROJECT_ID="YOUR_PROJECT_REF"


# =============================================================================
# SUPABASE EDGE FUNCTION SECRETS
# =============================================================================
# These are NOT set in .env — they must be set as Supabase secrets!
# Use the Supabase CLI or Dashboard to set these.
#
# CLI: supabase secrets set SECRET_NAME=value
# Dashboard: Project Settings → Edge Functions → Secrets
# =============================================================================

# -----------------------------------------------------------------------------
# AUTOMATIC SECRETS (Set by Supabase - no action needed)
# -----------------------------------------------------------------------------
# These are automatically available to all Edge Functions:
#
# SUPABASE_URL              - Your project URL
# SUPABASE_ANON_KEY         - Public/anon key
# SUPABASE_SERVICE_ROLE_KEY - Service role key (admin access)

# -----------------------------------------------------------------------------
# AI FEATURES - LOVABLE AI (Optional)
# -----------------------------------------------------------------------------
# Required for: AI Chat (Lovable provider), AI Text Generation, AI Page Migration
# Get your key from: Lovable Dashboard
#
# supabase secrets set LOVABLE_API_KEY=your-lovable-api-key
#
# LOVABLE_API_KEY=""

# -----------------------------------------------------------------------------
# AI FEATURES - FIRECRAWL (Optional)
# -----------------------------------------------------------------------------
# Required for: AI Brand Analysis, AI Page Migration (web scraping)
# Get your key from: https://firecrawl.dev
#
# supabase secrets set FIRECRAWL_API_KEY=your-firecrawl-api-key
#
# FIRECRAWL_API_KEY=""

# -----------------------------------------------------------------------------
# AI FEATURES - PRIVATE LLM (Optional, Alternative to Lovable)
# -----------------------------------------------------------------------------
# If you want to use your own OpenAI-compatible LLM instead of Lovable AI,
# configure these in the CMS Admin → Settings → AI Chat:
#
# - Private LLM Endpoint (e.g., https://api.openai.com/v1 or your own)
# - Private LLM Model (e.g., gpt-4, llama3, mistral)
# - Private LLM API Key (if required)
#
# No secrets needed here — configured in the CMS UI.

# -----------------------------------------------------------------------------
# LOCAL DEVELOPMENT (Optional)
# -----------------------------------------------------------------------------
# For local Supabase development (supabase start):
#
# VITE_SUPABASE_URL="http://localhost:54321"
# VITE_SUPABASE_PUBLISHABLE_KEY="<local-anon-key-from-supabase-start>"
