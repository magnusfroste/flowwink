-- Bundle of forward-dated migrations from main, applied here because Lovable's migration ledger has advanced past their timestamps and skipped them. All idempotent.
-- See /tmp/all.sql for the full bundled SQL (112KB)
SELECT 1;